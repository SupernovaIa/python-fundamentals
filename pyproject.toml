[project]
name = "python-fundamentals"
version = "0.1.0"
description = "A practical, hands-on introduction to Python for aspiring AI engineers"
requires-python = ">=3.10"

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
